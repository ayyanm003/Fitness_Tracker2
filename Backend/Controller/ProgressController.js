const Progress = require("../Model/Progress");

const addprogress = async ()=>{
    // const {user, weight, bodyFatPercentage, muscleMass, chest, waist, hips, arms, legs, runTime, }
try {
    
} catch (error) {
    
}
}